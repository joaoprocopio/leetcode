# https://leetcode.com/problems/two-sum/description/


def two_sum(nums: list[int], target: int) -> list[int]:
    result: list[int] = []

    for num in nums:
        result.append(num)

    return result
